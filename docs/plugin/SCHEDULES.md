# Schedules — overseerr-mcp

## Current state

overseerr-mcp does not define any scheduled tasks. It responds to on-demand tool calls only.

Potential scheduled tasks that could be added:
- Periodic check for failed requests and notification via Gotify
- Automatic retry of failed requests
- Availability monitoring for requested media
