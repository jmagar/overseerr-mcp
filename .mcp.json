{
  "mcpServers": {
    "overseerr-mcp": {
      "type": "http",
      "url": "${user_config.overseerr_mcp_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.overseerr_mcp_token}"
      }
    }
  }
}
